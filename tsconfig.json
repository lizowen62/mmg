{
  "files": [],
  "exclude": ["src/components/HelloWorld.vue"],
  "compilerOptions": {
    "noEmitOnError": false, // Le build se fait même avec des erreurs
    "strict": false,        // (optionnel) moins de vérifications strictes
    "skipLibCheck": true    // ignore les erreurs dans les fichiers .d.ts
  },
  "references": [
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    }
  ]
}
